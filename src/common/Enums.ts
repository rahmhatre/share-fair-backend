export enum JwtAPIConstants {
  ShareFairAPI = 'ShareFairAPI',
}

export enum AuthMode {
  GOOGLE = 'GOOGLE',
  CLASSIC = 'CLASSIC',
}

export enum UserType {
  ADMIN = 'ADMIN',
  USER = 'USER',
  DEVELOPER = 'DEVELOPER',
}

export enum UserStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum OrderStatus {
  PROCESSING = 'PROCESSING',
  DELIVERED = 'DELIVERED',
  ACTIVE = 'ACTIVE',
  CANCELLED = 'CANCELLED',
  TRANSIT = 'TRANSIT',
  PAYMENTDUE = 'PAYMENTDUE',
  PROBLEM = 'PROBLEM',
}
